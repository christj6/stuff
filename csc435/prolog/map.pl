color(red).
color(green).
color(yellow).
color(blue).

touching(a,b).
touching(a,c).
touching(a,d).

touching(b,a).
touching(b,c).
touching(b,f).

touching(f,b).
touching(f,c).
touching(f,e).

touching(e,c).
touching(e,d).
touching(e,f).

touching(d,a).
touching(d,c).
touching(d,e).

touching(c,a).
touching(c,b).
touching(c,d).
touching(c,e).
touching(c,f).
